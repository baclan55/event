{
  "name": "event-department-portal",
  "version": "1.0.0",
  "description": "Внутренний портал Event-отдела: FAQ, состав, правила, регламент, заявки и выговоры. Backend на Express + PostgreSQL (Neon), готов к деплою на Render.",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:migrate": "node src/db/migrate.js",
    "db:seed": "node src/db/seed.js",
    "setup": "npm run db:migrate && npm run db:seed"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-pg-simple": "^9.0.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "multer": "^2.2.0",
    "pg": "^8.11.5"
  }
}
