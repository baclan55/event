import { ProfileModerationInteractive } from '@/components/cabinet/InteractiveCore';

export const dynamic = 'force-dynamic';

export default function ProfileModerationPage() {
  return <ProfileModerationInteractive />;
}
