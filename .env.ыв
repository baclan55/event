# ============================================================================
# Скопируйте этот файл в .env и заполните значения.
# .env НЕ должен попадать в git (он уже в .gitignore).
# ============================================================================

# --- База данных -------------------------------------------------------
# Для Neon: Dashboard -> ваш проект -> Connect -> "Connection string" (psql).
# Обязательно оставьте ?sslmode=require в конце строки.
DATABASE_URL=postgresql://neondb_owner:npg_x5uavHEi9BSc@ep-muddy-heart-a2n24nmf-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# --- Сессии --------------------------------------------------------------
# Любая длинная случайная строка. Сгенерировать: openssl rand -hex 32
SESSION_SECRET=production

# --- Владелец портала ------------------------------------------------------
# Вход только через Discord. Узнайте свой Discord ID (Discord -> Настройки ->
# Расширенные -> включить "Режим разработчика", затем ПКМ по своему профилю
# -> "Копировать ID пользователя") и вставьте сюда. Когда вы войдёте через
# Discord, этот аккаунт автоматически получит права владельца.
DISCORD_OWNER_ID=

# --- Внешний вид/название портала (необязательно) -----------------------
APP_TITLE=Events Denver
APP_SUBTITLE=Ивент-отдел сервера
# Порог мероприятий в неделю, ниже которого счётчик горит красным
WEEKLY_EVENTS_TARGET=5

# --- Вход через Discord (обязательно — это единственный способ входа) ----
# Создайте приложение на https://discord.com/developers/applications
# Redirect URI должен быть: <ваш домен>/api/auth/discord/callback
DISCORD_CLIENT_ID=1534242952170307716
DISCORD_CLIENT_SECRET=ByCNdPxsvYj1aNfXKq9HIKGdaNKtty1T
DISCORD_REDIRECT_URI=https://events-denver.majes.workers.dev/api/auth/discord/callback

# --- Уведомления о заявках (необязательно) --------------------------------
# URL Discord Webhook (канал -> Настройки -> Интеграции -> Вебхуки) — если
# указан, при каждой новой заявке с сайта в этот канал придёт сообщение.
APPLICATIONS_WEBHOOK_URL=

# --- Прочее ---------------------------------------------------------------
PORT=3000
NODE_ENV=development

CLOUDINARY_CLOUD_NAME=dkumhqcnf
CLOUDINARY_API_KEY=212188664331275
CLOUDINARY_API_SECRET=Hw6-Gf_qc5NUqMOpAHX5j8rJ0NY